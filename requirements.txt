google-adk>=2.6.3
# The deploy verb builds its client with vertexai.Client, not google.genai.Client,
# and Agent Engine's runtime needs the same extras. ADK appends this line to the
# staged requirements if it is missing, so pinning it here is what keeps the local
# deploy environment and the container on one dependency list instead of two.
google-cloud-aiplatform[adk,agent_engines]>=1.163
pydantic>=2.7,<3
google-cloud-bigquery>=3.25
db-dtypes>=1.2
pyyaml>=6.0
pytest>=8.0
pypdf>=5.0
google-cloud-storage>=2.18
